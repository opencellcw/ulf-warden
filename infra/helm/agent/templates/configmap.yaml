apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent.fullname" . }}-config
  labels:
    {{- include "agent.labels" . | nindent 4 }}
data:
  SOUL.md: |
{{ .Values.agent.config.soul | indent 4 }}

  TOOLS.md: |
{{ .Values.agent.config.tools | indent 4 }}

  AGENTS.md: |
{{ .Values.agent.config.agents | indent 4 }}
